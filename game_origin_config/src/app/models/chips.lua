slot0 = class("Chip", require("app.models.base"))
slot1 = class("Chips", require("app.models.bases"))

function slot1.newModel(slot0, slot1)
	uv2 = "new"

	return slot2.new(slot0.game):init(slot1)
end

return slot1
